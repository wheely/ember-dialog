import { max } from 'ember-dialog/utils/zindex';
import { module, test } from 'qunit';

module('Unit | Utility | zindex');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = max();
  assert.ok(result);
});
